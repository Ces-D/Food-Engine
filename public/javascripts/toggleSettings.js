let toggleSettings = () => {
    let button = document.getElementById("settings-form");
    button.style.display == "none"
        ? (button.style.display = "block")
        : (button.style.display = "none");
};
